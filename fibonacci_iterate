#include <stdio.h>


int main()
{
   long a=0,b=1,c,n;
   scanf("%ld",&n);
   if(n>0)
   printf("%ld",a);
   while(n)
   {
        c=a+b;
        a=b;
        b=c;
        printf("%ld",c);
    n=n-1;
   }
   return 0;
}

